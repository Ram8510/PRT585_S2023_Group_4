@model IEnumerable<CDU_Music_Lessons_Application.Models.Duration>

@{
    ViewData["Title"] = "Choices of Duration and Price";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DurationTypeAndCost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DurationDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DurationTypeAndCost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DurationDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
